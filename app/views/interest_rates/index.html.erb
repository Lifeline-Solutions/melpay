<div class="max-w-7xl mx-auto p-6">
  <% if flash[:notice] %>
    <div class="mb-4 p-3 rounded bg-green-50 border border-green-200 text-green-800">
      <%= flash[:notice] %>
    </div>
  <% elsif flash[:alert] %>
    <div class="mb-4 p-3 rounded bg-red-50 border border-red-200 text-red-800">
      <%= flash[:alert] %>
    </div>
  <% end %>

  <div class="flex items-center justify-between mb-6">
    <h1 class="text-2xl font-semibold text-gray-900">Commission Rates</h1>
    <div>
      <%= link_to "Manage Rates", manage_interest_rates_path, class: "inline-block px-4 py-2 bg-indigo-600 text-white rounded shadow hover:bg-indigo-700" %>
    </div>
  </div>

  <!-- Global Rate Summary -->
  <div class="mb-6 bg-blue-50 border border-blue-200 rounded-lg p-4">
    <div class="flex items-center justify-between">
      <div>
        <p class="text-sm font-medium text-blue-900">
          <strong>Global Default Commission</strong>
          <span class="ml-2 text-lg font-semibold">
            <% if @system_setting.percentage? %>
              <%= number_to_percentage(@system_setting.global_interest_rate, precision: 2) %>
            <% else %>
              <%= number_to_currency(@system_setting.fixed_commission_amount) %>
            <% end %>
          </span>
          <span class="ml-2 text-sm font-normal text-blue-700">
            (<%= @system_setting.commission_type.titleize %>)
          </span>
        </p>
        <p class="text-sm text-blue-700 mt-1">
          Used by <%= @global_count %> clients
        </p>
      </div>
      <div class="text-sm text-blue-600">
        <%= link_to "Change Global Rate", manage_interest_rates_path, class: "hover:underline" %>
      </div>
    </div>
  </div>

  <!-- Clients Table -->
  <div class="overflow-x-auto bg-white rounded-lg shadow">
    <table class="min-w-full divide-y divide-gray-200">
      <thead class="bg-gray-50">
        <tr>
          <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Client</th>
          <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Rate Type</th>
          <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Applied Rate</th>
          <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Custom Rate</th>
          <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Type</th>
          <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Actions</th>
        </tr>
      </thead>

      <tbody class="bg-white divide-y divide-gray-200">
        <% @clients.each do |client| %>
          <tr class="hover:bg-gray-50">
            <td class="px-6 py-4 whitespace-nowrap">
              <div class="text-sm font-medium text-gray-900"><%= client.name %></div>
              <div class="text-xs text-gray-500"><%= client.email %></div>
            </td>

            <td class="px-6 py-4 whitespace-nowrap">
              <div class="text-sm text-gray-900">
                <%= client.effective_commission_type.titleize %>
              </div>
            </td>

            <td class="px-6 py-4 whitespace-nowrap">
              <div class="text-sm font-semibold text-gray-900">
                <% if client.effective_commission_type == 'percentage' %>
                  <%= number_to_percentage(client.effective_commission_value, precision: 2) %>
                <% else %>
                  <%= number_to_currency(client.effective_commission_value) %>
                <% end %>
              </div>
            </td>

            <td class="px-6 py-4 whitespace-nowrap">
              <div class="text-sm text-gray-500">
                <% if client.commission_type.present? %>
                  <% if client.percentage? && client.custom_interest_rate.present? %>
                    <%= number_to_percentage(client.custom_interest_rate, precision: 2) %>
                  <% elsif client.fixed? && client.fixed_commission_amount.present? %>
                    <%= number_to_currency(client.fixed_commission_amount) %>
                  <% else %>
                    <span class="text-gray-400">—</span>
                  <% end %>
                <% else %>
                  <span class="text-gray-400">—</span>
                <% end %>
              </div>
            </td>

            <td class="px-6 py-4 whitespace-nowrap">
              <% if client.commission_type.present? || client.custom_interest_rate.present? %>
                <span class="px-2 inline-flex text-xs leading-5 font-semibold rounded-full bg-yellow-100 text-yellow-800">Custom</span>
              <% else %>
                <span class="px-2 inline-flex text-xs leading-5 font-semibold rounded-full bg-blue-100 text-blue-800">Global</span>
              <% end %>
            </td>

            <td class="px-6 py-4 whitespace-nowrap">
              <div class="flex items-center space-x-2">
                <%= link_to "Edit Client", edit_client_path(client), 
                      class: "inline-block px-3 py-2 text-sm font-medium text-gray-700 bg-gray-100 border border-gray-300 rounded-md shadow-sm hover:bg-gray-200 hover:text-gray-900 transition-colors duration-200" %>
                
                <% if client.commission_type.present? || client.custom_interest_rate.present? %>
                  <%= button_to "Remove Custom", 
                        client_path(client), 
                        method: :patch,
                        params: { client: { 
                          commission_type: nil, 
                          custom_interest_rate: nil,
                          fixed_commission_amount: nil 
                        } },
                        data: { 
                          confirm: "Remove custom rate for #{client.name}?", 
                          turbo_confirm: "Remove custom rate for #{client.name}?" 
                        },
                        class: "inline-block px-3 py-2 text-sm font-medium text-red-700 bg-red-100 border border-red-200 rounded-md shadow-sm hover:bg-red-200 hover:text-red-800 transition-colors duration-200" %>
                <% end %>
              </div>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>

  <!-- Summary -->
  <div class="mt-4 text-sm text-gray-600">
    <p>
      Showing <%= @clients.count %> clients • 
      <strong><%= @custom_count %></strong> with custom rates • 
      <strong><%= @global_count %></strong> using global rate
    </p>
  </div>
</div>