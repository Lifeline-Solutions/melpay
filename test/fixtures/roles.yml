# Read about fixtures at https://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html
#
# Global (non-resource-scoped) rolify roles — resource_type/resource_id left nil,
# matching how Ability (app/models/ability.rb) checks user.has_role?(:role_name).

super_admin:
  name: super_admin

admin:
  name: admin

account_manager:
  name: account_manager

auditor:
  name: auditor
